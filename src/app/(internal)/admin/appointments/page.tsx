import { PagePlaceholder } from "@/components/layout/page-placeholder"

export default function AdminAppointmentsPage() {
  return (
    <PagePlaceholder
      eyebrow="Admin"
      title="Appointments"
      description="Appointments route shell set up for scheduling features in subsequent tasks."
    />
  )
}
