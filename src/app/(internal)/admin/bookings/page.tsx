import { PagePlaceholder } from "@/components/layout/page-placeholder"

export default function AdminBookingsPage() {
  return (
    <PagePlaceholder
      eyebrow="Admin"
      title="Bookings"
      description="Bookings workspace placeholder with shell-ready structure for upcoming modules."
    />
  )
}
