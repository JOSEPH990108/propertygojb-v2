import { PagePlaceholder } from "@/components/layout/page-placeholder"

export default function AdminSettingsPage() {
  return (
    <PagePlaceholder
      eyebrow="Admin"
      title="Settings"
      description="Workspace settings placeholder for configurable controls in future iterations."
    />
  )
}
