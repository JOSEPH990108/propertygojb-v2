import { PagePlaceholder } from "@/components/layout/page-placeholder"

export default function AgentBookingsPage() {
  return (
    <PagePlaceholder
      eyebrow="Agent"
      title="Bookings"
      description="Agent bookings placeholder route with clean, reusable layout baseline."
    />
  )
}
