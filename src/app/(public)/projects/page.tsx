import { PagePlaceholder } from "@/components/layout/page-placeholder"

export default function ProjectsPage() {
  return (
    <PagePlaceholder
      eyebrow="Public"
      title="Projects"
      description="Project directory placeholder. Filters, sorting, and data integration will be introduced later."
    />
  )
}
